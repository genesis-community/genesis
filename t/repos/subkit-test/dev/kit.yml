---
subkits:
- prompt: "What blobstore do you want to use?"
  type: blobstore
  choices:
    - subkit: webdav
      label: Use the non-HA webdav backend
      default: yes
    - subkit: s3
      label: Use the awesome HA AWS-managed and never fails S3 backend
