resources:
- name: genesis-ci-dockerhub
  type: docker-image
  source:
    username:   (( grab meta.dockerhub.username ))
    password:   (( grab meta.dockerhub.password ))
    repository: (( grab meta.image.ci-remote ))
