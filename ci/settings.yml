---
meta:
  name:    genesis
  release: Genesis
  target:  pipes/genesis
  url:     https://pipes.scalecf.net

  initial_version: 2.8.10

  # we build our own image in this pipeline, and
  # use it, instead of standard concourse images.
  image:
    name: registry.ops.scalecf.net/genesis-community/genesis-ci
    src-image: registry.ops.scalecf.net/genesis-community/genesis
    src-tag: latest

  aws:
    access_key:  ((cloudfoundry-community-aws.access_key_id))
    secret_key:  ((cloudfoundry-community-aws.secret_access_key))

  git:
    name:  Genesis CI Bot
    email: genesis-ci@rubidiumstudios.com

  vault:
    url:       ((vault.url))
    token:     ((vault.token))

  github:
    owner:        genesis-community
    repo:         genesis
    branch:       v2.8.x-dev
    main-branch:  main
    private_key:  ((github.private_key))
    access_token: ((github.access_token))

  image-registry:
    email:    ((docker.email))
    username: ((docker.username))
    password: ((docker.password))
    repository: registry.ops.scalecf.net/genesis-community/genesis

  shout:
    url:      ((shout.url))
    username: ((shout.username))
    password: ((shout.password))

