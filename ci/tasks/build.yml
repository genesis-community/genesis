---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: registry.ops.scalecf.net/genesis-community/genesis-ci

inputs:
- name: version
- name: git
- name: git-ci

outputs:
- name: build

params:

run:
  path: git-ci/ci/scripts/build


